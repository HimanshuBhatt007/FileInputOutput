package FileWriterConstructor;

public class FileWriterConstructor {
    public static void main(String[] args) {

        /* FileWriter is used to write data to the file.
        There are in total 4 constructor in FileWriter class and if the file specified to the constructor is not available then it
        does not give IOException it makes one for us.

        constructor for overriding existing data

        1. FileWriter fw = new FileWriter(String filename);
        2. FileWriter fw = new FileWriter(File f);


        constructor for appending the existing data

        1. FileWriter fw = new FileWriter(String filename, boolean) true for appending the data
        2. FileWriter fw = new FileWriter(File f, boolean);



         */

    }
}
